apiVersion: apps/v1
kind: Deployment
metadata:
  name: hetdeployment
  labels:
    app: myapp
    tier: frontend
spec:
  # modify replicas according to your case
  replicas: 3
  selector:
    matchLabels:
      tier: frontend
  template:
    metadata:
      labels:
        tier: frontend
    spec:
      containers:
      - name: mynode
        image: hetmistry/task5.1p:latest
        ports:
        - containerPort: 8080